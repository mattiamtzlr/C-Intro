#ifndef BAR_H
#define BAR_H

/* The above makes sure that this header file is only ever included once,
 * by first checking whether the definition BAR_H has been set, and if not,
 * setting it and continuing as always. Otherwise, the whole file will
 * be ignored. */

// add two numbers together
int add(int, int);

#endif
